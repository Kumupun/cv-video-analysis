apiVersion: 1

providers:
  - name: CV Pipeline
    orgId: 1
    folder: CV Pipeline
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
